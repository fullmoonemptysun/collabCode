{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": "moon",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "build:ui": "rm -rf dist && cd ../client && npm run build && cp -r dist ../server",
    "deploy:all": "npm run build:ui && git add . && git commit -m 'uibuild' && git push origin"

  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
